# Идея
Дообучить маленькую GPT-модель на аудиторский язык (логика системы управления рисками бизнес-процессов и проектов + термины - именно внутренний аудит)

# Мотивация
Получить компетенции в области дообучения:
- разобраться с разными подходами дообучения (Continued pretraining, SFT, Preference tuning)
- разобраться в разных подходах дообучения через изменение весов модели (LoRA, QLoRA)
- практика работы с тензорами-весами моделей
- практика инференса модели
- разобраться в факторах, влияющих на работу модели (по скорости, памяти)

# Вызовы
- нет обучающих данных
    - решение - сгенерировать синтетические (дистиляция стиля крупной модели)
- непонятно, как оценивать результаты
    - подготовить eval датасет, но он снова будет синтетическим
    - llm-as-judge
    - human evaluation (но это было в предыдущих проектах, хочется конкретных значений)
- непонятно, какой подход подойдет лучше (надо попробовать несколько)


# Поиск существующих датасетов
После неглубокого поиска стало понятно, что рассчитывать на готовые датасеты не стоит. Тогда придется генерировать синтетику. Да и плюс в рамках проекта-практики не стоит сильно заморачиваться над качеством пайплайна получения данных. 


# Генерация обучающего датасета
> Главный риск: галлюцинации модели учителя попадут в обучающий датасет

1) Использовать две разные модели: задает вопросы + отвечает
2) Использовать несколько моделей
    -  задают вопросы несколько + отвечает 1 модель
    -  1 модель задает вопросы + отвечают несколько
    -  задают вопросы несколько + отвечают несколько
3) Использовать одну модель

| Стратегия | Разнообразие вопросов | Консистентность стиля ответов | Сложность пайплайна | Под стилевой SFT | Ранг для тебя |
|---|---|---|---|---|---|
| **1) Одна спрашивает + другая отвечает** | Среднее | Высокая (1 учитель) | Низкая | Хорошо | 🥈 2 |
| **2a) Несколько спрашивают + одна отвечает** | Высокое | Высокая (1 учитель) | Средняя | Оптимально | 🥇 1 |
| **2b) Одна спрашивает + несколько отвечают** | Среднее | Низкая (стили смешаны) | Средняя | Плохо* | 4 |
| **2c) Несколько спрашивают + несколько отвечают** | Очень высокое | Низкая (стили смешаны) | Высокая | Плохо* | 5 |
| **3) Одна модель (спрашивает и отвечает)** | Низкое (распределение схлопывается) | Высокая | Минимальная | Слабо | 🥉 3 |

## Генерация вопросов
### Проблема
Если вопросы генерит одна модель «просто так», получим несколько тысяч однообразных, гладких, похожих друг на друга вопросов. Модель свалится в узкое распределение, и дообученная малая модель будет хороша ровно на этом узком срезе.

### Решение
Заранее "разметить" поле предметной области на сегменты знаний - заставлять модель не просто из воздуха генерировать тысячи вопросов, а именно по конкретным разделам, методологиям, кейсам


## Генерация ответов
### Проблема
Модель для ответов быстро выходит за лимиты токенов (если использовать последнюю топовую проприетарную, а как раз стиль таких сильных моделей нам и нужен), а также с течением времени качество ответов может падать и скатываться в однообразие

### Решение
Чтобы не раздувать контекст модели, надо подавать датасет батчами


# Дообучение модели
Моделью для обучения была выбрана [qwen3-4b-instruct-2507](https://huggingface.co/Qwen/Qwen3-4B-Instruct-2507)

Для первого эксперимента (для быстрой проверки эффекта от дообучения) была использована готовая библиотека [peft](https://pypi.org/project/peft/) и выбран метод LoRA

Логи дообучения представлены в файле ```peft_lora_report.md```

trainable params: 33,030,144 || all params: 4,055,498,240 || trainable%: 0.8145

То есть вместо обучения 4 миллиардов параметров, мы обучаем лишь 33М, что составляет меньше 1% от всего числа параметров.

|ВРЕМЯ ОБУЧЕНИЯ||
|---|---|
| Всего (train+eval+save): | 385.4 мин (23125 с) ~ 6.5 часов |
|  Среднее на эпоху:       | 192.7 мин (11560 с) ~ 3.2 часа |
|  Всего optimizer-шагов:  | 306 |
|  Среднее на шаг:         | 61.65 с (эффективный батч = 8 примеров) |
|  ~на микро-батч:         | 7.71 с (1 примера) |
|  ~на пример:             | 7.71 с |

## Результаты дообучения
> Ключевые моменты (саммари):
> - для дообучения хватает одной эпохи на датасете 1.5к записей
> - в базовом режиме когда макбук засыпал (потухал экран), процесс приостанавливался, поэтому нужно прописывать ```caffeinate -i```
> - все обучение заняло 6.5 часов
> - eval съел ~20% времени - слишком много, надо оптимизировать

| Стадия (процент от 200% = двух эпох) | eval_loss | train_loss |
| :--- | :--- |:-|
| **0%** |        | 2.259|
| **33%** | 1.476 | 1.487|
| **66%** | 1.251 | 1.281|
| **99%** | 1.097 | 1.116|
| **131%** | 1.048 | 0.9065|
| **164%** | 1.025 | 0.903|
| **197%** | 1.015 | 0.9169|
| **200%** | 1.015 | |

### Лосс
- Eval_loss монотонно падает и выходит на плато в конце (1.025 → 1.015 → 1.015). Это значит: 2 эпохи — почти оптимум, дальше выигрыш был бы копеечный, а риск переобучения рос бы. Да и на самом деле уже после 1 эпохи лосс упал до 1.097 - то есть оптимально даже было прогонять только одну эпоху.

- Это даже можно отследить по train_loss - в первую эпоху у нас лосс упал с 2.259 до 1.116 (эффект в 1.143): резкое уменьшение на первых шагах и замедление к концу эпохи. А на второй эпохи медленно уменьшался лосс, а под конец вообще начал колебаться (0.8856->1.094->0.903->0.9344->0.9733->0.9169). То есть эффект меньше 0.2 всего.

- grad_norm стабильно 0.7–1.5, без скачков → обучение устойчивое, расхождения нет.

- learning rate корректно затухает по линейному графику с ~1e-4 до ~0

### Время обучения
- TimingCallback = 23 125 с ≈ 6.4 ч, шаг ~62 с.
- train_runtime (Trainer) = 62 190 с ≈ 17.3 ч, и tqdm показал [17:16:34].

Разница ~11 часов — потому что mac засыпал во время обучения и приостанавливал работу всех запущенных процессов. Но заметил и понял я это уже под конец (в последний час обучения). Для решения этой проблемы необходимо либо выделять процесс через его id, либо в принципе для всех процессов прописывать ```caffeinate -i```

Каждый eval ~670 с. Их было 7 штук → ~77 минут из 6.4 ч ушло только на оценку (305 примеров по одному, без KV-кэша — медленно).

## Расчеты затрат на обучение
Обучение было реализовано на MacBook Air:
```
Железо:
- Чип: Apple M5
- Общее количество ядер: 10 (4 супер, 6 эффективности)
- Память:	24 ГБ
Модель: qwen3-4b-instruct-2507 = 4.02 млрд параметров
Данные:
- train: 1523 × 0.8 = 1218
- eval: 1523 × 0.2 = 305
- 1218 примеров × 2 эпохи = 2436 проходов, средняя длина ~500 токенов → ~1.2 млн токенов обучения
LoRA: обучали 33 млн (0.81%)
На MPS: чистого счёта 6.4 ч, ~62 с/шаг → эффективная производительность ~1 TFLOP/s (Apple GPU крайне неэффективен на тренировке)
```
### Сравнение времени дообучения
GPU	| Память|Время (LoRA, 2 эпохи)	|Комментарий
|---|---|---|---|
Apple Silicon M5 (MacBook, MPS)	|24 GB 	|6.5 ч	|Средний шаг ~61.65 с — на порядок медленнее CUDA; eval особенно тяжёлый (eval_runtime 470–780 с против ~33 с на A100)
Nvidia A100-SXM4-80GB (1 карта)	|85 GB	|21 мин	|~18× быстрее Mac на этой задаче
Nvidia A100 (8 карт, device_map='auto')	|85 GB/карта	|26 мин	|На ~19% медленнее одной карты из-за оверхеда межкарточной синхронизации при шардинге 4B модели
Nvidia A100 (1 карта, batch_size=4, grad_accum=2)	|85 GB	|9.3 мин	|В предыдущих экспериментах batch=1, grad_accum=8, что не до конца загружало ресурсы карты, то есть где-то половина ресурсов простаивала. Сейчас она была забита под завязку
Nvidia A100 (1 карта, batch_size=4, grad_accum=2, sdpa)	|85 GB	|9.4 мин	|
||**Эксперименты с Unsloth**|
|**_далее все эксперименты с dropout=0.05 (блокировал оптимизации по времени)_**|
Nvidia A100 (1 карта, Unsloth, batch_size=4, grad_accum=2,)	|85 GB	|7 мин	| Большие трудности с установкой библиотеки - много конфликтов с transformers, datasets
Nvidia A100 (1 карта, Unsloth, batch_size=4, grad_accum=2, group by length)	|85 GB	|6.2 мин	| Группировка примеров по длине внутри одного батча дало буст - мы меньше тратим времени на обработку ненужных паддингов у коротких текстов, которые попали с длинными в батч
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=4, grad_accum=2, group by length)	|85 GB	|6.9 мин	| 
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=8, grad_accum=2, group by length)	|85 GB	|5.2 мин	| Увеличение батча дало прирост в скорости + надо увеличивать lr (1e-4 -> 2e-4) так как обновления текут более гладко
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=16, grad_accum=2, group by length)	|85 GB	|4.6 мин	|
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=32, grad_accum=2, group by length)	|85 GB	|4.3 мин	|
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=64, grad_accum=2, group by length)	|85 GB	|4.4 мин	| Train loss уже не так сильно падает - с 1.8 до 1.15
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=128, grad_accum=2, group by length)	|85 GB	|4.7 мин	|
|dropout=0|
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=4, grad_accum=2, group by length)	|85 GB	|6.1 мин	| Был убран dropout, который блокировал временные ускорения и оставлял только оптимизации по памяти
Nvidia A100 (1 карта, Unsloth, Liger kernel, batch_size=32, grad_accum=2, group by length)	|85 GB	|3.6 мин	| 
Nvidia A100 (1 карта, Unsloth, batch_size=32, grad_accum=2, group by length)	|85 GB	|3.2 мин	| ТОП результат

#### Почему с увеличением batch size время обучения перестало падать?
Этот парадокс — GPU Saturation Bottleneck (эффект перенасыщения GPU).
- в батче много текста разной длины, отсюда и много паддинга
- процессор (CPU) перестал успевать склеивать так много примеров (Data Loader Bottleneck)

### LoRA на облачном GPU — время и стоимость
На NVIDIA эффективность в 30–100× выше MPS. Время = объём вычислений / реальная производительность карты:

GPU	| Память |	Эффект. произв. |	Время (LoRA,2эпохи) |	Цена/час (spot/community) |	Стоимость прогона
|---|---|---|---|---|---|
RTX 4090|	24 GB	|~50 TFLOP/s	|~10–20 мин|	$0.3–0.7|	~$0.10–0.25
L4 A10	|24 GB|	~30 TFLOP/s	|~15–25 мин	|$0.4–1.0	|~$0.15–0.40
Tesla P40	|24 GB|	~3–5 TFLOP/s (только fp32)|	~2–3.5 часа	|$0.15–0.40|	~$0.50–1.20
A100 (40/80GB)|	40–80 GB|	~95 TFLOP/s	|~8–15 мин	|$1.5–2.5	|~$0.25–0.60
H100 (80GB)	|80 GB	|~150 TFLOP/s	|~5–10 мин	|$2–3	|~$0.25–0.50

Вывод по LoRA: на любой современной карте это минуты и буквально центы–меньше доллара (с учётом времени на запуск/скачивание весов — пара долларов максимум). Для LoRA хватает даже 24 GB карты; с 4-bit QLoRA будет вообще 12–16 GB.

Важно: компьютинг почти одинаков с MPS (~29 PFLOP), просто NVIDIA работает на таких объемах в десятки раз быстрее.

> CPU на такой задаче выдаёт обычно 0.05–0.2 TFLOP/s — то есть в 5–20× медленнее MPS: пришлось бы ждать 2-5 суток

### Обучение ВСЕХ 4 млрд параметров (full fine-tuning)
Здесь упор не во время, а в память. Бюджет VRAM для полного дообучения с Adam (mixed precision):

Что хранится|Байт/параметр	|Для 4B
|-|-|-|
Веса (bf16)	|2	|8 GB
Градиенты (bf16)|	2	|8 GB
Master-копия весов (fp32)|	4|	16 GB
Состояния Adam — m и v (fp32)|	8|	32 GB
Итого без активаций	|16	|~64 GB
| + активации (зависит от batch/seq)|	—	|~5–20 GB
ВСЕГО	| |	~70–85 GB

Для нашего датасета full FT не нужен и вреден — 4B параметров на 1.2 млн токенов почти гарантированно переобучатся.